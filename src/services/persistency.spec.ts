describe('Testando um number', () => {
  it('Should return one', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('Testando uma string', () => {
  test('Should return Lucas', () => {
    const nome = 'Lucas';
    expect(nome).toBe('Lucas');
  });
});
